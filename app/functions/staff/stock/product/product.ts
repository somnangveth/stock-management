"use server";

import { createSupabaseServerClient } from "@/lib/supbase/action";

export async function addProductStaff(){
    const supabase = await createSupabaseServerClient();

    try{
        
    }catch(error){

    }
}